#include "isoengine/common/clock.h"

namespace iso
{

// Clock::Clock()
// {
//     lastUpdateTime = ClockType::now();
// }

// Duration::rep Clock::restart()
// {
//     auto dt = ClockType::now() - lastUpdateTime;
//     lastUpdateTime = ClockType::now();
//     return std::chrono::duration_cast<Duration>(dt).count();
// }

} // namespace iso